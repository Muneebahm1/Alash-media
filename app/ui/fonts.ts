import {Inter,Lusitana,Lato} from 'next/font/google';

export const inter = Inter({subsets: ['latin']});
export const lusitana = Lusitana({weight:['700','400'],subsets: ['latin']});
export const lato = Lato({weight: ['400'],subsets:['latin']});

export const verdana = {

    font_Family: 'Verdana, Lato, Helvetica, sans-serif',
}